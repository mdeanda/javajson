description = 'javajson-mapper is an object mapper for javajson'

dependencies {
	compile project(':parser')

	compile 'org.slf4j:slf4j-api:latest.integration'
	testCompile 'org.slf4j:slf4j-simple:latest.integration'
}

